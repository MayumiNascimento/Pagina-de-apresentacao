import { SafeUrl } from '@angular/platform-browser';

export interface Servicos{
    id?: number;
    nome?: string;
    tipo?: string;
    descricao?: string;
    imagem?: string;
    preco?: string;
}